name: build and push and deploy

on:
  push:
    branches:
      - qa

permissions:
  contents: read
  packages: write


jobs:
  build-and-deploy:
    name: Build & push
    runs-on:
      - self-hosted

    environment: ${{ github.ref_name }}

    steps:
      - uses: actions/checkout@v3

      - name: Wyświetl napis
        run: echo "HELLOO"



